# 更新记录
> 约定：每次提交/推送代码时，都需同步更新本文件，记录涉及的子系统、日期（含具体时间）与关键改动，便于版本追踪。 
> 约定：历史记录保持只追加，不修改已记录的日期与内容。
> 约定：按顺序向下填写



## 2025-11-27 14:10 — 终端登录与助手修复
- **feat:** 新增仿终端风格的登录覆盖层，包含身份认证、描述输入及头像选择（`index.html`/`index.css`/`modules/login.js`）。
- **ui:** 登录界面支持中英双语（当前已本地化为中文），并适配了 CRT 扫描线与输入动画效果。
- **fix:** 修复悬浮助手（派蒙）在页面跳转或窗口缩放时的定位问题，将其改为相对于应用容器的绝对定位，并强制在登录后重置位置（`pages/subject_monitor.js`）。

## 2025-11-27 13:50 — 预览空状态居中修复
- **ui:** 修复“备用遮蔽舱”占位卡片中文本未垂直居中的问题，通过重置内部容器的 Flex 属性实现完全居中（`index.css`）。

## 2025-11-27 13:45 — 采集耗尽可跳日
- **gameplay:** 新增跳日条件：当每日采集许可耗尽（0/10）时，即使未到 12:00 也允许手动进入下一天（`pages/subject_monitor.js`）。
- **ui:** 更新时间栏提示文案为“12:00 或 许可耗尽后开启”（`index.html`）。

## 2025-11-27 13:40 — 提示文案与样式精简
- **ui:** 提示气泡改为单行不换行显示，字号缩小至 10px，内边距收紧，提升精致感（`index.css`）。
- **content:** 精简了实验体、日志、稳定剂的提示文案，确保在单行模式下不过长（`index.html`）。

## 2025-11-27 13:30 — 提示样式统一与修复
- **ui:** 统一所有“i”字提示图标为圆角方框样式，包括实验体列表、监控日志及时间栏提示（`index.css`）。
- **fix:** 修复提示气泡文本竖向排列问题，改为横向最大宽度自适应（`index.css`）。
- **ui:** 移除时间栏临时的文本标签，恢复并优化为 Tooltip 形式（`index.html`）。

## 2025-11-27 13:20 — 提示气泡下移
- **fix:** 将时间栏提示气泡改为在按钮下方展开，靠边时也能完整显示（`index.css`）。

## 2025-11-27 13:19 — 提示气泡防溢出
- **fix:** 时间栏提示气泡改为可换行且限制最大宽度，避免靠边时溢出屏幕（`index.css`）。

## 2025-11-27 13:15 — 跳日规则与提示
- **ux:** “下一天”按钮需到当日 12:00 后才可点击，未达时间弹出提示；时间栏旁新增“！”提示说明规则（`pages/subject_monitor.js`/`index.html`/`index.css`）。
- **ui:** HUD 调试入口隐藏，防止干扰主界面（`index.css`）。
- **docs:** `docs/systems/subject_monitor.md` 补充���日规则与 HUD 隐藏说明。

## 2025-11-27 13:10 — 悬浮助手初始排版修复
- **fix:** 保持头像锚点不变，文本长度变化只移动气泡，不再把头像推出屏幕，左右/上下均钳制在可视范围内（`pages/subject_monitor.js`）。

## 2025-11-27 13:09 — 悬浮助手位置收敛
- **fix:** 拖拽结束或文本更新时自动重新钳制位置，长文本不会再超出可视区域（`pages/subject_monitor.js`）。

## 2025-11-27 13:08 — 悬浮助手气泡单行显示
- **fix:** 调整气泡文本为强制单行显示，避免靠近边缘时换行（`index.css`）。

## 2025-11-27 13:07 — 悬浮助手头像比例修复
- **fix:** 悬浮助手靠近边缘时头像保持圆形，不再被挤压为椭圆（`index.css`）。

## 2025-11-27 13:02 — 悬浮助手气泡宽度上限
- **ui:** 悬浮窗气泡随文本自适应，增加最大宽度限制与换行，避免长句撑破布局（`index.css`）。

## 2025-11-27 12:53 — 悬浮助手（派蒙）提示
- **feat:** 新增可拖动的右下悬浮窗，头像缓慢旋转，显示 `data/paimon_messages.js` 的短语；任一实验体当日状态为红色时高亮提示并指出异常实验体；拖动时仅显示头像，释放后根据所在半区自动将气泡置于左/右侧（`index.html`/`index.css`/`pages/subject_monitor.js`/`data/paimon_messages.js`/`docs/*`）。

## 2025-11-27 11:16 — 样式文件改名为 index.css
- **chore:** 将页面样式文件重命名为 `index.css`，并更新所有引用与文档；旧文件 `subject_monitor.css` 已移除（`index.html`/`index.css`/`docs/*`/`UPDATE_LOG.md`）。

## 2025-11-27 11:12 — 入口文件切换为 index
- **chore:** 新增 `index.html` 作为默认入口，文档与更新记录内的引用全部从 `subject_monitor.html` 切换至 `index.html`（`index.html`/`docs/*`/`UPDATE_LOG.md`）。

## 2025-11-25 10:10 — HUD 标记点更新
- **chore:** 根据最新调试坐标更新 S-01 的 HUD 标记点与偏移（`data/hud_frames.js`）。

## 2025-11-25 10:05 — 预览页页脚清理
- **ui:** 移除监控屏底部 footer 内容，保持预览/监控区域更紧凑（`index.html`/`index.css`）。

## 2025-11-25 10:00 — 波形尺寸微调
- **ui:** 心率/脑电波形卡片整体缩小（宽度和高度减小、内边距收紧）以适配当前视窗（`index.css`）。

## 2025-11-25 09:55 — 预览界面精简
- **ui:** 移除预览态的屏幕标题、底部状态/RW注射时长，以及预览卡片中的“阶段”文案仅保留代号、名称与进度条（`index.html`/`pages/subject_monitor.js`/`index.css`）。

## 2025-11-25 09:50 — 页面缩放至免滚动
- **layout:** 去除滚动并将页面缩至 1200x900，同时居中显示，保证全部内容无需滚动可见（`index.css`）。

## 2025-11-25 09:45 — 页面可视与滚动
- **layout:** 保持 1300x1000 固定尺寸，但允许页面整体滚动，确保底部按钮可见（`index.css`）。

## 2025-11-25 09:40 — 固定页面尺寸
- **layout:** 将监控页面固定为 1300x1000，便于后续HUD小框定位（`index.css`）。

## 2025-11-25 09:35 — 实验体HUD数据框
- **feat:** 在监控屏轮廓周围渲染透明青色HUD数据框，连线指向轮廓标记点，显示按实验体定制的小数据点（数据源 `data/hud_frames.js`），预览界面隐藏，选中实验体显示（`index.html`/`index.css`/`pages/subject_monitor.js`）。
- **docs:** `docs/systems/subject_monitor.md` 更新HUD说明。

## 2025-11-25 09:30 — 波形排版与尺寸优化
- **ui:** 波形卡片数值移至标题右侧，并增大波形显示区域宽度/高度，提升可读性（`index.html`/`index.css`）。

## 2025-11-25 09:25 — 波形平滑跳变优化
- **ux:** ECG/EEG 波形在体征刷新时采用平滑幅度/速度过渡，避免突兀重置，仍随数值变化（`index.css`/`pages/subject_monitor.js`）。
- **docs/process:** 保持历史记录不改动，新的改动写在最新条目。

## 2025-11-25 09:19 — 波形与扫描线调整
- **ux:** ECG/EEG 波形仅在进入单个实验体监控时显示，预览界面不展示（`index.html`/`pages/subject_monitor.js`）。
- **ui:** 监控屏扫描线动画覆盖整屏，从采集操作触发；右下新增 ECG/EEG 波形卡片随体征更新波速与数值（`index.css`/`index.html`/`pages/subject_monitor.js`）。
- **ui:** 体征条与预览进度条配色统一（心率青、脑电紫、异化蓝；异常为红），预览模式后台刷新心率/脑电同步展示（`index.css`/`pages/subject_monitor.js`）。
- **fix:** 监控屏容器补充 `id="monitor-screen"`，避免动效控制报错；扫描线范围与视觉框一致（`index.html`/`index.css`）。
- **docs:** `docs/systems/subject_monitor.md` 同步波形、扫描线与预览进度条说明。

## 2025-11-24 16:30 — 波形显示范围与日志日期纠正
- **docs/process:** 此条为新日志，保持历史记录不改动。
- **ux:** ECG/EEG 波形仅在进入单个实验体监控时显示，预览界面不展示（`index.html`/`pages/subject_monitor.js`）。
- **ui:** 监控屏扫描线动画范围覆盖整个屏幕，视觉与红框一致（`index.css`）。
- **fix:** 监控屏容器补充 `id="monitor-screen"` 以避免动效 `classList` 报错（`index.html`）。
- **feat:** 点击“采集数据”触发监控屏扫描线动画一次（`pages/subject_monitor.js`/`index.css`/`index.html`/`docs/systems/subject_monitor.md`）。
- **ui:** 监控界面三条大体征进度条配色与预览小条保持一致（心率青、脑电紫、异化蓝；异常时为红）（`index.css`）。
- **ui:** 监控屏右下新增 ECG/EEG 波形卡片，实时显示心率/脑电的波形与数值（`index.html`/`index.css`/`pages/subject_monitor.js`/`docs/systems/subject_monitor.md`）。
- **ux:** 预览模式下后台定期刷新各实验体心率/脑电并同步进度条，预览卡片竖排三条无数值进度条，颜色心率青/脑电紫/异化蓝，异常时红（`pages/subject_monitor.js`/`index.css`/`docs/systems/subject_monitor.md`）。

## 2025-11-24 16:05 — 采集日志进度条 + 跳日回预览
- **ui:** 采集数据日志条本身作为进度条背景，长度对应当前异化百分比，移除单独的“异化进度 +X%”文案（`pages/subject_monitor.js`/`index.css`）。
- **ux:** 点��“下一天”后自动返回预览界面，便于日切后重新选择实验体（`pages/subject_monitor.js`）。
- **docs:** `docs/systems/subject_monitor.md` 更新采集日志高亮说明。
- **ui:** 预览卡片右下角新增 3 条无数值进度条，分别对应最近记录的心率、脑电和异化程度（`pages/subject_monitor.js`/`index.css`/`docs/systems/subject_monitor.md`）。

## 2025-11-24 16:00 — 采集日志进度条
- **ui:** 采集数据日志新增异化进度条直观显示当前异化百分比；保留淡青高亮样式（`pages/subject_monitor.js`/`index.css`）。
- **docs:** `docs/systems/subject_monitor.md` 更新采集日志高亮说明。

## 2025-11-24 15:55 — 采集日志精简与样式更新
- **ui:** 采集数据日志不再附带剩余许可与研究进度，仅保留采集核心信息；采集日志样式更新为淡青描边与柔和背景，进一步区隔普通记录（`pages/subject_monitor.js`/`index.css`）。
- **docs:** `docs/systems/subject_monitor.md` 增补采集日志高亮说明。

## 2025-11-24 15:50 — 采集日志高亮
- **ui:** 采集数据生成的日志添加专属高亮条与淡蓝背景，便于从普通记录中区分（`subject_monitor.js`/`index.css`）。

## 2025-11-24 15:45 — 提示图标样式同步
- **ui:** 信息提示圈统一字体/字重并关闭父级大写影响，确保三处提示均显示为小写“i”（`index.css`）。

## 2025-11-24 15:42 — 提示图标样式
- **ui:** 信息提示圈采用正文字体与较粗笔画，显示为小写 “i” 而非看似大写 “I”，提升辨识度（`index.css`）。

## 2025-11-24 15:40 — 提示气泡补充
- **ui:** 在实验体列表与监控日志标题旁添加信息提示：说明异化满值后不可采集/刷新，以及日志颜色含义（红=异常高风险，橙=次级风险，常规=普通记录）。（`index.html`）

## 2025-11-24 15:35 — 稳定剂提示修复
- **ui:** 稳定剂提示改为单一圆圈“i”，悬停出现自定义说明气泡，避免双圈且确保提示可见（`index.html`/`index.css`）。

## 2025-11-24 15:30 — 稳定剂提示
- **ui:** 稳定剂右侧新增“？(信息)”圆形提示，悬停显示说明（`index.html`/`index.css`）。

## 2025-11-24 15:27 — 资源区间距调整
- **ui:** 资源区对齐方式改为紧凑左对齐，使“采集许可”与数值紧贴展示（`index.css`）。

## 2025-11-24 15:25 — 资源区文案收紧
- **ui:** 采集许可文案去除括号与每日次数说明，稳定剂/许可保持紧凑单行展示（`index.html`/`index.css`）。

## 2025-11-24 15:20 — 采集许可与稳定剂、研究进度
- **feat(gameplay):** 新增每日采集许可上限（10 次/天），日切自动重置，超额采集会被阻止。
- **feat(gameplay):** 新增稳定剂（初始 3 支），可预先待命并抵消下一次采集产生的异化增长。
- **feat(meta):** 采集带来的异化增长会同步累积“研究进度”，按增长值的 10% 计入，稳定剂抵消后不增加。
- **ui:** 监控界面增加资源状态区，显示每日许可、稳定剂状态与研究进度条。
- **docs:** 更新 `docs/systems/subject_monitor.md` 说明资源与进度的新规则。

## 2025-11-24 15:05 — 修复预览界面“丢失”状态显示Bug
- **fix(ui):** 修复了当一个实验体丢失后，返回预览界面时，整个屏幕被“已丢失”遮罩覆盖的Bug。
- **feat(ui):** 现在，只有丢失实验体自己的预览卡片会显示“LOST”状态，其他卡片保持正常。
- **JS:** 更新了 `showPreview` 和 `renderPreviewGrid` 函数，以正确管理和渲染每个卡片的“丢失”状态。
- **CSS:** 为预览卡片新增了 `.is-lost` 状态样式。

## 2025-11-24 15:00 — 新增“花屏”动效
- **feat(fx):** 新增了一个覆盖全监控屏的、间歇性闪现的“花屏”（电视噪点）效果，以增强氛围感。
- **refactor(fx):** 轮廓动画恢复为平滑的移动和闪烁，移除了“急冲”效果，与新的“花屏”效果分离，确保两种动画互不干扰。
- **JS:** 重构了动画启动逻辑，现在由 `startEffectLoops` 统一管理轮廓自身的平滑动画和一个独立的、用于触发“花屏”的随机计时器。

## 2025-11-24 14:50 — 最终动效重构：追求极致平滑
- **refactor(fx):** 对实验体轮廓动画系统进行最终重构，以实现绝对的渲染平滑，彻底解决“卡顿”和“黑框”问题。
  - **CSS:** 动画逻辑被简化为只驱动 `transform` 和 `opacity` 两个最高性能的属性。
    - 移除了所有 `clip-path` (glitch) 动画，因其是潜在的性能瓶颈和渲染错误的来源。
    - 创建了一个全新的 `@keyframes combined-movement`，将“移动”和“抖动”效果合并到单个 `transform` 动画中，杜绝了属性控制冲突。
    - 最终动画由 `combined-movement` 和 `flicker-long` (控制 `opacity`) 分层叠加，确保了动画的流畅性和视觉丰富性。
  - **JS:** 无需改动，继续保持仅切换 `.is-animating` 类的简单逻辑。

## 2025-11-24 14:14 — 更新系统文档
- **docs:** 更新 `docs/systems/subject_monitor.md` 技术说明。
  - 同步了新的动态备注系统和“刷新周期采集许可”冷却机制的文档。
  - 补充了关于UI已全面重构的说明。

## 2025-11-24 11:20 — 重构采集冷却机制 & 优化提示
- **refactor(gameplay):** 重构了数据采集的冷却机制，以更好地匹配游戏节奏。
  - 废弃了基于全局时间戳的冷却系统。
  - 实现了“刷新周期采集许可”机制：每次生命体征刷新后，玩家获得一次采集机会，周期内多次采集将被阻止。
- **style(ui):** 将“采集频率过高”的提示从日志输出改为按钮的视觉警告（���动、变色），避免刷屏。

## 2025-11-24 11:00 — 新增多样化采集备注
- **feat(narrative):** 新增了基于异化增长的动态备注系统，以取代原有的固定采集日志。
  - **数据层:** 为每个实验体在 `data/subjects.js` 中添加了独立的、分等级的（低/中/高）备注池。
  - **逻辑层:** 现在采集数据时，会根据异化进度 `inc` 的增长幅度，从对应实验体的备注池中抽取一条不重复的备注进行显示。

## 2025-11-24 10:45 — UI微调
- **style(ui):** 调整部分面板标题的字体为等宽字体，以增强科技感。
- **style(ui):** 缩小“异常日志”按钮的字号，使布局更协调。

## 2025-11-24 10:40 — UI修复
- **fix(ui):** 调整新手教程遮罩透明度，降低背景暗度，提升非高亮区域内容的可读性。

## 2025-11-24 10:35 — 文档维护
- **chore(docs):** 调整 `UPDATE_LOG.md` 的条目顺序，确保所有日志按时间降序排列，最新日志在最上方。

## 2025-11-24 10:30 — UI全面优化与修复
- **feat(ui):** 对监控终端界面进行全面的UI/UX优化。
    - 引入 `Roboto Mono` 和 `Noto Sans SC` 字体，增强科技感与可读性。
    - 使用CSS自定义属性（变量）重构样式，统一了颜色、字体和间距，便于维护。
    - 采用Flexbox重构主布局，使其更现代化和灵活。
    - 重新设计了所有面板、卡片和按钮的样式，使其外观更精致。
- **fix(ui):** 修复了在快捷预览界面中，实验舱网格无法正常显示的Bug。

## 2025-11-23 21:31 — 采集冷却优化
- 刷新生命体征后，下一次采集可跳过 5s 冷却限制（`pages/subject_monitor.js`）。
- 文档同步：`docs/systems/subject_monitor.md` 说明冷却跳过规则。

## 2025-11-23 21:29 — 教程完成条件补充
- Step 5 下点击“快捷预览”将自动结束教程，除“完成”外的另一快速退出路径（`pages/subject_monitor.js`/`docs/tutorial.md`）。

## 2025-11-23 21:26 — 教程高亮范围调整
- Step 2/4 高亮区域限定为生命体征条（心率/脑电/异化），避免遮罩覆盖过大区域（`pages/subject_monitor.js`）。

## 2025-11-23 21:24 — 教程采集与按钮逻辑
- Step 4 再次点击“采集数据”即可进入 Step 5，符合“采集至满”流程；Step 5 仅显示“上一步”“完成”按钮（`pages/subject_monitor.js`）。
- 教程文档同步更新推进规则（`docs/tutorial.md`）。

## 2025-11-23 21:19 — 教程完善
- 修复“下一步”无法从 Step 4 到 Step 5 的问题，统一用控制函数推进/回退（`pages/subject_monitor.js`）。
- 新增 `docs/tutorial.md` 记录完整教程逻辑；`docs/README.md` 添加教程导航。

## 2025-11-23 21:16 — 教程步骤联动
- 选择实验体后自动推进新手教程到“等待异常”步骤（`pages/subject_monitor.js`），避免卡在 Step 1。

## 2025-11-23 21:12 — 教程遮罩修复
- 补充遮罩元素引用（`guide-overlay`），修复教程初始化报错（`index.html`/`pages/subject_monitor.js`）。

## 2025-11-23 21:11 — 教程遮罩镂空
- 教程遮罩对高亮目标位置打孔（随目标动态调整），保证左侧列表等高亮区域完全不被遮挡，同时保持背景变暗（`index.css`/`pages/subject_monitor.js`）。

## 2025-11-23 21:08 — 教程高亮透光
- 降低整体遮罩透明度、移除高亮底色并强化光晕，保证左侧列表等被高亮区域更易看清（`index.css`）。

## 2025-11-23 21:07 — 教程高亮提亮
- 保持背景遮罩变暗，但为高亮区域增加半透明底与更强光晕，确保高亮区域和提示文字清晰可见（`index.css`）。

## 2025-11-23 21:05 — 教程遮罩调整
- 仅保留整体遮罩使背景变暗，`guide-card` 和 `guide-highlight` 提升层级不被遮罩，突出提示内容与高亮区域（`index.css`）。

## 2025-11-23 21:03 — 教程提示无遮罩
- 移除教程高亮的遮罩阴影，仅保留描边高亮，提示文字不再被遮挡（`index.css`）。

## 2025-11-23 21:00 — 教程提示框高亮
- 教程提示框增加描边与光晕，提升对比度与可读性（`index.css`）。

## 2025-11-23 20:59 — 教程高亮可读性
- 调整新手教程高亮遮罩为透明镂空，确保被高亮区域文本清晰可见（`index.css`）。

## 2025-11-23 20:52 — 新手教程引导
- 新增遮罩式新手教程，步骤覆盖选择实验体、等待异常、采集数据、异化进度、快捷预览，高亮目标区域，可跳过/返回（`index.html`/`pages/subject_monitor.js`/`index.css`）。
- 文档同步：`docs/systems/subject_monitor.md` 补充新手教程行为。

## 2025-11-23 20:47 — 预览内框精简
- 移除预览舱卡内部框线，仅保留可染色的外层卡框，视觉更简洁（`index.css`）。

## 2025-11-23 20:41 — 预览快捷与状态染色
- 监控按钮区新增“快捷预览”，快速返回实验舱预览网格。
- 预览网格舱卡同步当日状态颜色（白/橙/红），与信息卡保持一致。
- 文档更新：`docs/systems/subject_monitor.md` 记录新按钮与预览染色行为。

## 2025-11-23 20:24 — 时间倍率与呈现更新
- 时间流保持 1 秒现实 = 2 分钟游戏，但时间栏以每 0.5 秒 +1 分钟方式跳动，维持压迫感且避免大步进（`modules/time.js`）。
- 文档同步：`docs/systems/time_system.md`、`docs/systems/subject_monitor.md`、`docs/architecture.md`、`docs/README.md` 调整时间节奏描述。

## 2025-11-23 20:22 — 时间显示节奏微调
- 时间流改为 1 秒现实 = 1 分钟游戏，时间栏每秒跳动 1 分钟以提升沉浸感（`modules/time.js`）。
- 文档同步：`docs/systems/time_system.md`、`docs/systems/subject_monitor.md`、`docs/architecture.md`、`docs/README.md` 恢复对应说明。

## 2025-11-23 20:21 — 时间倍率调整
- 时间系统节奏改为 1 秒现实 = 2 分钟游戏（`modules/time.js`），自动推进与跳日保持不变。
- 文档同步：`docs/systems/time_system.md`、`docs/systems/subject_monitor.md`、`docs/architecture.md`、`docs/README.md` 更新时间倍率描述。

## 2025-11-23 20:09 — 文档目录整理
- 文档移动至 `docs/` 分层：`docs/systems/`（系统）、`docs/world/`（世界观）、`docs/architecture.md`（架构）。
- 新增 `docs/README.md`（导航与约定）与 `docs/templates/doc_template.md`（文档模板）。
- 更新 `time_system.md` 引用路径，其他历史日志未改动。

## 2025-11-23 19:45 — 日志格式补充
- 约定补充：后续新增记录需写具体时间（含时分），历史记录不改动。

## 2024-11-23 — 实验体监控终端（subject_monitor）
- 日志上限调整为 18；游戏开始/日切/跳日清空当前日志后写入当日状态。
- 日常状态：按白/橙/红概率 70%/20%/10% 分配事件池（20/10/5 条），状态日志用文字颜色标识。
- 异化封顶：异化 100% 时监��屏噪点遮罩显示“已丢失观测结果”，心率/脑电/日常状态停止更新，采集/刷新提示观测结果已丢失。
- 调试序列：采集×2 → 刷新生命体征×3 → 采集×1，可快速将当前实验体异化置为 100%。
- 文档更新：`subject_monitor.md` 同步上述行为与展示文案。 

## 2024-11-23 — 架构模块化与清理
- 入口与模块：`index.html` 改用 `<script type="module">` 加载 `pages/subject_monitor.js`，主逻辑拆分到模块。
- 时间与数据：时间系统抽成 `modules/time.js` 并暴露订阅接口；实验体与日常状态池移到 `data/subjects.js`。
- 资源清理：删除演示文件 `time_system.html`/`time_system.css`，统一使用模块化时间栏。
- 文档：新增 `architecture.md` 说明分层，`time_system.md` 描述模块用法，本文件追加本次改动记录。 
- 观察备注展示移除，信息卡仅展示当日状态文本（无“今日状态”前缀）。
- 预览占位文案改为“选择实验体后，将在此显示当日状态”。
- 文档更新：`subject_monitor.md` 说明信息卡行为调整。 

## 2024-11-23 — 信息卡状态染色
- 信息卡当日状态文本随状态染色（白/橙/红），丢失时显示“已丢失观测结果”并标红。
- 文档更新：`subject_monitor.md` 描述信息卡颜色随状态变化。 

## 2024-11-22 — 实验体监控终端（subject_monitor）
- 生命体征与异化逻辑调整：体征刷新不再提升异化，异化仅在采集时按异常随机累加（单异常 +0.0~10.0%，双异常 +5.0~15.0%，5s 冷却）。切换实验体保留各自异化值。
- 日志系统优化：普通日志上限 20 条；异常日志持久保存，使用同一日志框切换查看。
- 预览与监控：预览四宫格显示红外轮廓并具备柔和动态；监控屏展示不同实验体红外图，支持采集/刷新体征。
- 文档更新：`subject_monitor.md` 补充上述逻辑、UI 状态与扩展说明。 
- 样式与时间系统：样式抽离至 `index.css` 便于审查； 


- 2025-11-27 14:30 — 登录界面动效增强
  - **feat(fx):** 新增背景网格滚动动画，增强空间纵深感。
  - **feat(fx):** 状态提示文本（如“系统待机中”）采用逐字打字机效果，光标闪烁。
  - **ui:** 优化输入框聚焦时的下划线扫描动效与按钮点击的“故障”反馈。
  - **anim:** 优化登录步骤切换的淡入淡出过渡，以及登录成功后的“扫描线”离场动画。

- 2025-11-27 14:40 — 动效节奏微调
  - **fix(fx):** 调慢打字机动画速度（由 20-30ms 调整至 40-50ms），使文字显现更具节奏感和易读性。

- 2025-11-27 15:10 — 战斗系统基础实现
  - **feat(combat):** 新增回合制战斗系统，包含属性克制、技能判定与胜负结算逻辑。
  - **feat(data):** 创建战斗专用数据库，录入首批实验体（小咪/马楼/炉心犬等）及其技能与形态数据。
  - **ui:** 新增神经同步（战斗）覆盖层，包含双方状态HUD、立绘占位与战斗日志控制台。
  - **gameplay:** 实现基础养成逻辑，战斗胜利可获得经验值并触发升级/进化。

- 2025-11-27 15:40 — 战斗系统体验优化
  - **ui:** 分离战斗与管理入口至右侧悬浮菜单，新增实验体管理终端查看属性/技能。
  - **gameplay:** 战斗胜利后实验体自动恢复满血。
  - **fx:** 战斗中新增技能伤害飘字与抛硬币动效。
  - **sync:** 监控列表名称现在会同步显示战斗系统中的进化后名称。

- 2025-11-27 16:00 — 战斗系统功能修复与补全
  - **fix:** 修复战败后血量未重置的问题，现在无论胜负均回满血量。
  - **fix:** 修复硬币动效被遮挡的问题（提升 z-index）。
  - **feat(data):** 补全所有实验体及其技能的详细数据描述，从 2 只扩充至 12 只。
  - **feat(cultivation):** 初始玩家现在获得 S-01, S-07, S-13 三只基础实验体。

- 2025-11-27 16:20 — 交互与逻辑修复
  - **fix(combat):** 修复“挠”等特殊技能无伤害的问题，现在正确进入伤害计算流程。
  - **ui:** 移除所有原生 alert/confirm 弹窗，替换为与游戏风格一致的自定义模态框。
  - **ux:** 战斗中途退出现在使用异步确认框，体验更流畅。
